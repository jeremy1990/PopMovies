<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <ListPreference
            android:key="@string/pref_list_sort_by_key"
            android:title="@string/pref_title_sort_by"
            android:defaultValue="@string/pref_default_sort_by"
            android:entries="@array/pref_list_sort_by_titles"
            android:entryValues="@array/pref_list_sort_by_values"
            android:negativeButtonText="@null"
            android:positiveButtonText="@null"/>

</PreferenceScreen>
